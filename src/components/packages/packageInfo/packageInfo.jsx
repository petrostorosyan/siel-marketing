import styles from "./packageInfo.module.scss";

const PackageInfo = () => {
  return (
    <div className={styles.infoContainer}>
      <div className={styles.infoWrapper}>
        You can customize and adjust these packages to suit your brand’s unique
        needs, creating an ideal fit for your goals.
      </div>
    </div>
  );
};

export default PackageInfo;
